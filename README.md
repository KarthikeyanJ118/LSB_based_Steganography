# LSB_based_Steganography
The Objective of this project is to encode the secret data in an image at the sender end and decode the secret data at the receiver end, the encoding is done by manipulating pixel data at bit level and authentication using magic string.
